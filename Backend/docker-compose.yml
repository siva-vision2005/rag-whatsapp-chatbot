version: "3.8"

services:
  whatsapp-bot:
    build: .
    container_name: whatsapp-rag-chatbot
    restart: always
    ports:
      - "3000:3000"
    volumes:
      - whatsapp_session:/app/auth_info_baileys
    env_file:
      - .env

volumes:
  whatsapp_session:
    driver: local
